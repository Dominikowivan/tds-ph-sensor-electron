{
  "name": "ph-tds-sensor-electron",
  "version": "1.0.0",
  "description": "PH and TDS reader for liquids using electron and Johnny five",
  "main": "window.js",
  "scripts": {
    "postinstall": "./node_modules/.bin/electron-rebuild -v $(./node_modules/.bin/electron -v)",
    "start": "./node_modules/.bin/electron .",
    "watch": "./node_modules/.bin/electron . --watch"
  },

  "dependencies": {
    "electron": "^18.3.7",
    "johnny-five": "^0.10.13"
  },
  "devDependencies": {
    "serialport": "^7.1.5",
    "electron-rebuild": "^1.5.11",
    "electron-reload": "^1.2.1"
  }
}
